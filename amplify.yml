version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd nba-vision-next
        - npm install
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: nba-vision-next/out
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
